import 'package:flutter/material.dart';

class AppColorsLight {
  // اللون الأساسي للتطبيق
  static const Color primary = Color(0xFF152D45);
  
  // لون الخلفية
  static const Color background = Color(0xFFE7E0D0);

  // لون النصوص الأساسية
  static const Color textPrimary = Color(0xFFFD6050);

  // لون النصوص الثانوية
  static const Color textSecondary = Color(0xFF2A6C69);

  // لون الأزرار
  static const Color button = Color(0xFF0C141A);
}
